source 'https://rubygems.org'

gem 'jekyll'
gem 'jekyll-seo-tag'
gem 'github-pages'
gem 'webrick'

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]
gem 'wdm', platforms: [:mswin, :mingw] if RUBY_VERSION < '3.2'